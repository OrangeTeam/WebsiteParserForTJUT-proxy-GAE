dependencies {
    compile 'org.orange:website-parser-tjut:2.0'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId 'website-parser-tjut-proxy-api'
        }
    }
}
